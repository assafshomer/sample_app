<h1>Listing users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 30 %><br/>
      <%= link_to user.name, href: user_path(user) %>
    </li> 
  <% end %>
</ul>

<br />

<%= link_to 'New User', new_user_path %>