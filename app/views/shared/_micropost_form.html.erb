<%= form_for(@micropost) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<% if in_reply_to_id %>
		<%= render 'shared/replied_to_micropost', form: f, in_reply_to_id: in_reply_to_id %>
	<% else %>
		<%= render 'shared/new_micropost', form: f %>
	<% end %>
<% end %> 